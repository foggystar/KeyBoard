# 高度客制化分体键盘：内置外设技术大纲

> 目标：整理所有直接接入键盘系统、并且在 USB 与 BLE 模式下都应正常工作的内置外设。  
> 不包含：USB Hub 下游临时接入的普通 USB 外设。

---

## 1. 系统边界

### 主机半边负责

- 键盘按键扫描与全局状态聚合
- USB HID / BLE HID 输出
- 与副半边的 BLE / 有线总线通信
- 触控板、触摸屏、旋钮、传感器等外设数据汇总
- 电源模式、休眠、唤醒与故障管理

### 副半边负责

- 本地按键扫描
- 本地旋钮、灯光或传感器
- 分离时通过 BLE 向主机半边发送事件
- 合体时通过有线总线向主机半边发送事件

### 统一原则

所有外设都应先接入键盘主控或本地协处理器，再由主控转换为：

- USB Composite HID
- BLE Composite HID
- Vendor-defined 配置通道

---

## 2. 外设总览

| 外设 | 推荐接口 | 是否需要独立控制器 | 无线模式 |
|---|---|---:|---:|
| 按键扫描移位寄存器 | SPI 类串行接口 | 否 | 支持 |
| 触控板 | I²C / SPI | 通常否 | 支持 |
| 触摸屏触控控制器 | I²C / SPI | 否 | 支持 |
| 显示屏 | SPI / RGB / MIPI | 复杂屏幕建议独立处理器 | 支持 |
| FOC 力反馈旋钮 | SPI / UART / CAN | 是 | 支持 |
| 旋转编码器 | GPIO / 定时器 | 否 | 支持 |
| RGB 灯光 | 单线 / SPI | 大规模灯效可独立控制 | 支持但耗电高 |
| 运动传感器 | I²C / SPI | 否 | 支持 |
| 环境光传感器 | I²C / ADC | 否 | 支持 |
| 距离/接近传感器 | I²C / GPIO | 否 | 支持 |
| 蜂鸣器/提示音 | PWM / I²S | 功放可独立 | 支持 |
| 电池计量与充电状态 | I²C / ADC / GPIO | 电源管理芯片 | 支持 |
| 分体合体检测 | GPIO / 霍尔传感器 | 否 | 支持 |

---

## 3. 按键扫描系统

### 设计思路

使用移位寄存器减少 GPIO 占用，主控只需约 4–5 根信号：

- `SCAN_CLK`
- `SCAN_DATA_OUT`
- `SCAN_DATA_IN`
- `SCAN_LATCH`
- 可选 `SCAN_IRQ`

### 推荐功能

- 按键矩阵扫描
- 消抖
- NKRO
- 多层键位
- 宏触发
- 主副半边完整状态同步

### 核心问题

- 扫描周期与功耗之间的平衡
- 长链路上的信号完整性
- 上电时输出状态是否确定
- 分体断线后防止卡键
- 合体/分离切换时发送完整键位位图

---

## 4. 触控板

### 推荐连接

优先使用带原始坐标输出的触控板控制器，通过：

- I²C：引脚少、实现简单
- SPI：刷新率更高、延迟更低
- 中断脚：有触摸事件时唤醒主控

### 输出方式

主控将触控数据转换为：

- USB Mouse / Precision Touchpad 类 HID
- BLE Mouse / Touchpad HID
- 手势对应的 Consumer Control

### 可实现功能

- 光标移动
- 单击、双击
- 双指滚动
- 缩放、切换桌面等手势
- 区域化快捷操作

### 核心问题

- Windows Precision Touchpad 协议比普通鼠标 HID 复杂
- BLE 下高频坐标传输会增加功耗
- 触控板 FPC 与电机、USB、天线之间需要隔离
- 需要统一处理有线与无线报告格式

---

## 5. 触摸屏与显示屏

### 触摸控制器

通常通过 I²C/SPI 接主控，提供：

- 触点坐标
- 按压状态
- 手势中断
- 多点触控信息

### 显示接口选择

#### 小型简单屏幕

适合直接由主控驱动：

- 128×64 单色 OLED
- 240×240 小型 SPI LCD
- 简单状态、图标、菜单

#### 中大型复杂屏幕

建议增加图形协处理器：

- 480×272、480×480、800×480
- 动画、图片、复杂 GUI
- RGB、MIPI-DSI 等接口
- 大帧缓冲或双缓冲

### 核心问题

- 屏幕和背光通常是无线续航的主要负担
- 显示系统应允许完全断电
- 触摸输入不应依赖 GUI 正常运行
- GUI 卡死不能影响基本键盘功能
- 显示资源应放在 QSPI Flash 或 TF 卡

---

## 6. FOC 力反馈旋钮

### 推荐架构

不要让键盘主控直接运行高速 FOC 电流环。

```text
键盘主控
   │ SPI / UART / CAN
   ▼
STM32G4 / STSPIN32G4
   │
三相驱动 + 电流采样 + 编码器
   │
无刷电机
```

### 主控下发

- 目标位置
- 虚拟卡点数量与间距
- 阻尼
- 摩擦
- 惯量
- 最大扭矩
- 当前模式

### FOC 控制器返回

- 绝对角度
- 旋转增量
- 速度
- 按压状态
- 温度
- 电流和故障状态

### 核心问题

- 电机电源与数字电源需要隔离和充分去耦
- 电流环必须独立于 BLE、USB 和 GUI 延迟
- 磁铁和电机不能紧邻 2.4 GHz 天线
- 需要硬件过流、过温和欠压保护
- 无线模式下应限制最大扭矩以降低电池峰值电流

---

## 7. 普通旋转编码器与机械输入

### 接口

- A/B 相接 GPIO 或定时器编码器接口
- 按压开关接普通 GPIO
- 必要时增加 RC 滤波或施密特触发

### 用途

- 音量
- 页面切换
- 时间轴
- 灯光亮度
- FOC 模式选择

### 核心问题

- 机械抖动
- 快速旋转时漏计数
- 深度休眠唤醒
- 与 FOC 旋钮事件格式统一

---

## 8. RGB 灯光

### 方案选择

#### 少量状态灯

- 普通 RGB LED
- PWM 控制
- 功耗最低、实现简单

#### 单键可寻址 RGB

- WS2812/SK6812 类单线灯
- 或 SPI 类可寻址灯
- 独立 5 V 电源域

### 推荐策略

- USB 模式允许完整灯效
- BLE 模式降低亮度和刷新率
- 长时间无输入自动关闭
- LED 电源使用负载开关彻底断电

### 核心问题

- RGB 功耗通常远高于 MCU
- 大量 LED 需要单独电源预算
- 单线信号需要电平兼容
- 高速灯效会占用 CPU 和通信带宽
- 电源噪声不能影响触控板和射频

---

## 9. 运动传感器

### 推荐器件类型

优先使用超低功耗三轴加速度计，而不是完整六轴 IMU。

### 可实现功能

- 移动键盘自动唤醒
- 静止后进入低功耗
- 抬起、倾斜或翻转手势
- 敲击外壳触发
- 跌落或搬运记录

### 接口

- I²C 或 SPI
- 至少一个中断脚
- 可选第二个事件中断

### 核心问题

- 传感器方向在不同模组/主板中保持一致
- 电机振动可能导致误触发
- 唤醒阈值和静止时间需要可配置
- 默认应运行在微安级常开模式

---

## 10. 环境光、接近与姿态辅助传感器

### 环境光传感器

用途：

- 自动调节屏幕亮度
- 自动调节 RGB 亮度
- 暗环境关闭高亮指示灯

接口：I²C 或 ADC。

### 接近传感器

用途：

- 手靠近时唤醒屏幕
- 离开后快速熄屏
- 非接触手势

接口：I²C + 中断。

### 霍尔传感器

用途：

- 检测主副半边是否合体
- 检测旋钮或磁性附件位置
- 作为无机械触点的模式开关

### 核心问题

- FOC 电机磁场可能干扰霍尔或磁力计
- 接近传感器需避免被外壳和玻璃遮挡
- 传感器应支持硬件中断，减少轮询

---

## 11. 音频与提示反馈

### 简单提示音

- 压电蜂鸣器
- PWM 驱动
- 用于连接、低电量、模式切换提示

### 更复杂音频

- I²S DAC / Class-D 功放
- 小型扬声器
- 支持按键音、提示音或媒体控制反馈

### 核心问题

- 音频不是键盘核心功能，建议作为可选模块
- 功放应支持关断
- 扬声器磁体应远离天线和磁编码器
- 音频数据占用 Flash/TF 卡空间

---

## 12. 电池、充电和电量计量

### 推荐组成

- 单节锂电池充电管理
- 电源路径管理
- 3.3 V 稳压
- 电池电量计
- USB VBUS 检测
- 充电状态输入
- 各外设电源负载开关

### 主控需要获取

- 电池电压
- 剩余容量
- 充电状态
- USB 是否接入
- 电池温度
- 欠压或故障状态

### 核心问题

- 主机半边可能同时给副半边充电
- USB Hub 和外设可能显著增加有线功耗
- BLE 模式下必须关闭 Hub 和不必要的电源域
- 电池电压不能只靠简单 ADC 线性估算容量
- FOC 旋钮的峰值电流需要独立评估

---

## 13. 分体通信与合体检测

### 分离状态

```text
副半边
   │ 自定义 BLE GATT
   ▼
主机半边
   ├─ BLE HID → 电脑
   └─ USB HID → 电脑
```

副半边只向主机发送内部事件，不直接作为独立键盘连接电脑。

### 合体状态

推荐使用差分有线总线，例如半双工 RS-485：

- 电源
- GND
- A/B 差分线
- Dock Detect
- 可选唤醒或模块识别脚

### 链路切换核心流程

```text
检测到合体
→ 开启有线收发器
→ 握手
→ 同步完整按键和外设状态
→ 主机确认
→ 关闭副半边 BLE
```

拆分时反向处理，并发送 `release-all` 防止卡键。

### 核心问题

- 热插拔
- 连接器触点抖动
- BLE 与有线链路短时同时存在
- 数据包序列号和丢包检测
- 重连后的完整状态同步
- 主副半边固件版本兼容

---

## 14. 外设内部事件格式

建议所有外设先转换成统一内部事件，而不是直接生成 USB 报告。

```text
事件来源
事件类型
设备编号
时间戳
序列号
数据长度
有效载荷
校验
```

典型事件：

- `KEY_DOWN`
- `KEY_UP`
- `POINTER_MOVE`
- `TOUCH_CONTACT`
- `ENCODER_DELTA`
- `KNOB_POSITION`
- `GESTURE`
- `BATTERY_STATUS`
- `DEVICE_FAULT`

优点：

- USB 与 BLE 共用同一套输入逻辑
- 主副半边共用同一套协议
- 便于录制、回放和调试
- 新增外设时不需要重写整个 HID 层

---

## 15. 电源域建议

建议将以下部分划分为独立可控电源域：

| 电源域 | 无线待机 |
|---|---:|
| 键盘主控与 BLE | 保持 |
| 按键扫描 | 保持或降频 |
| 副半边有线收发器 | 关闭 |
| USB Hub | 关闭 |
| 显示处理器 | 关闭 |
| 屏幕背光 | 关闭 |
| RGB 灯 | 关闭 |
| TF 卡 | 关闭 |
| FOC 功率级 | 关闭或限功率 |
| 触控板 | 低功耗/中断模式 |
| 加速度计 | 超低功耗常开 |

核心原则：

> 能通过中断唤醒的外设，不要持续轮询；  
> 能完全断电的高耗电模块，不要只依赖软件休眠。

---

## 16. 外设优先级建议

### 第一阶段：基本输入平台

- 按键扫描
- 主副半边 BLE / 有线通信
- USB HID
- BLE HID
- 普通旋转编码器
- 电池检测

### 第二阶段：高级输入

- 触控板
- FOC 旋钮
- 合体检测
- 运动唤醒
- RGB 状态灯

### 第三阶段：交互与显示

- 触摸屏
- 图形界面
- TF 卡主题与资源
- 环境光和接近传感器
- 音频提示

---

## 17. 最需要优先验证的问题

1. 主机半边能否稳定同时维持电脑 BLE HID 和副半边 BLE 链路。
2. USB、BLE 两种输出模式下，所有内置外设的事件是否一致。
3. 合体/分离切换是否会出现卡键、丢键或重复事件。
4. 触控板在 BLE 下的延迟、刷新率和功耗是否可接受。
5. FOC 电机噪声是否影响射频、触控和电源稳定性。
6. 显示屏、RGB、FOC 开启时的电池峰值电流。
7. 各电源域关闭后是否存在反向供电或漏电。
8. 天线与金属定位板、电池、屏幕和电机的相对位置。
9. 主副半边不同固件版本下的协议兼容。
10. 外设故障时，基础键盘功能是否仍能正常工作。

---

## 18. 推荐的总体结构

```text
主机半边主控
├─ 按键扫描
├─ 触控板
├─ 触摸控制器
├─ 显示处理器（可选）
├─ FOC 控制器
├─ 普通旋钮
├─ RGB
├─ 运动/环境传感器
├─ 电池与电源管理
├─ BLE → 电脑
├─ USB → USB Hub → 电脑
├─ BLE → 副半边
└─ 有线总线 → 副半边
```

设计重点不是让主控直接驱动所有负载，而是：

- 主控统一聚合输入
- 高实时性任务交给独立协处理器
- 高功耗模块具有独立电源控制
- USB 与 BLE 共用统一事件模型
- 外设故障不影响基本键盘输入
