# 高度客制化分体键盘技术大纲（精简版）

## 1. 项目目标与边界

### 目标

- 分体式键盘，支持 **USB 有线 HID** 与 **BLE 无线 HID**。
- 主机半边包含 USB Hub；主控作为 Hub 的一个下行 USB Device 连接电脑。
- 副半边：
  - 分离时通过 BLE 向主机半边传输输入；
  - 合体时切换为有线总线。
- 主机半边统一处理两侧按键、触控输入、FOC 旋钮事件、宏和层，并向电脑输出一个逻辑 HID 设备。
- 使用同一公共主板，对比：
  - `ESP32-S3-WROOM-1`
  - 自研裸 `STM32WB55` 射频模组

### 当前不处理

- 不把 USB Hub 下游的任意外设转换或桥接到 BLE。
- 不采用 STM32WB5MMG。
- 不要求 STM32WB55 模组与任意 WROOM 底板完全兼容；只服务于本项目定义的公共主板。

---

## 2. 总体架构

```text
电脑
├─ 有线：USB-C → USB Hub → 主控模组 → USB Composite HID
└─ 无线：主控模组 → BLE Composite HID

主机半边
├─ 可替换主控模组
│  ├─ ESP32-S3-WROOM-1
│  └─ 自研 STM32WB55 模组
├─ 主半边按键扫描与输入设备
├─ USB Hub
├─ 分体有线收发器
├─ 电池、充电和电源路径
└─ 与独立 FOC 控制器通信

副半边
├─ 本地扫描和输入处理
├─ 分离：BLE → 主机半边
└─ 合体：有线总线 → 主机半边
```

主机半边维护整个键盘的**权威输入状态**。副半边不直接连接电脑，也不独立生成最终 HID 报告。

---

## 3. 主控选型思路

### ESP32-S3-WROOM-1

优点：

- 原生 USB Device，USB HID 开发成熟。
- 可同时作为 BLE HID Peripheral 连接电脑，并作为 BLE Central 连接副半边。
- 性能、内存和软件生态更适合快速迭代、复杂界面和功能扩展。
- 使用现成射频模组，硬件风险低。

核心问题：

- BLE 常连接和浅睡眠功耗较高。
- 无线分体时需要维持两条 BLE 连接，续航压力更明显。
- 电源需要承受较高的瞬态峰值电流。

### 自研 STM32WB55 模组

优点：

- 更适合长期 BLE 连接和低功耗运行。
- 同样支持 USB Device、BLE HID 和主从双角色连接。
- 适合在功能趋于稳定后验证长续航产品路线。

核心问题：

- 必须自行处理 2.4 GHz 射频、晶振、匹配、滤波、天线和 EMC。
- STM32CubeWB、CPU2 无线栈和低功耗调试复杂度更高。
- 算力和内存余量低于 ESP32-S3，不适合承担复杂图形界面。

### 当前策略

- **ESP32-S3：完整功能和快速开发基线。**
- **STM32WB55：低功耗和产品化对照。**
- 最终通过同一公共主板上的实测结果选型，不提前淘汰任一方案。

---

## 4. 可替换计算模组

### 机械设计

- STM32WB55 模组尽量采用与 ESP32-S3-WROOM-1 相同的外形包络、安装区域和天线方向。
- 两种模组的半孔焊盘安装半孔针或排针，公共主板安装插座，实现快速更换。
- 天线端统一朝向键盘外缘，公共主板只保留一套天线净空区。
- 半孔针和插座不得进入天线近场区域。

### 兼容原则

兼容的是**功能接口**，不是芯片 GPIO 编号。

主板使用如下名称：

```text
USB_DP / USB_DM
SCAN_CLK / SCAN_DATA_OUT / SCAN_DATA_IN / SCAN_LATCH
SPLIT_TX / SPLIT_RX / SPLIT_DE
I2C_SCL / I2C_SDA
SPI_SCK / SPI_MOSI / SPI_MISO
MODULE_EN_N / BOOT_REQUEST_N
DEBUG_DIO / DEBUG_CLK
BAT_SENSE / VBUS_SENSE
```

不要把主板设计建立在 ESP32 GPIO Matrix 的任意路由能力上。应先完成 STM32WB55 的固定 Alternate Function 映射，再为 ESP32-S3 选择对应 GPIO。

### 必须内部适配的差异

- **USB：**公共焊盘固定为 D+、D-，两种模组内部映射到各自 USB 引脚。
- **EN/NRST：**统一作为复位或禁止运行信号，不假设两者都能通过该脚实现完全断电。
- **BOOT：**ESP32 与 STM32 启动逻辑不同，由模组内部反相或适配。
- **调试：**ESP32 使用 USB/JTAG 或其调试接口；STM32 使用 SWD，统一映射到公共调试针脚。

---

## 5. 硬件设计核心问题

### 公共主板

公共主板固定包含：

- USB Hub、USB-C 接口和 ESD；
- 键盘扫描移位寄存器；
- 分体有线收发器；
- 电池、充电、电源切换和测量点；
- 屏幕、触控、FOC 控制器等外围接口；
- 主控模组插座。

公共 3.3 V 电源按 **ESP32-S3 最坏峰值和瞬态响应**设计，使两种模组均能稳定工作。

### STM32WB55 模组

模组内部至少包含：

- STM32WB55；
- 32 MHz HSE 与 32.768 kHz LSE；
- 去耦、内部 SMPS 外围和 VDDUSB 处理；
- 官方参考射频匹配与谐波滤波；
- 板载 2.4 GHz 天线；
- SWD、复位和启动适配；
- 可选屏蔽罩焊盘。

核心风险：

1. **射频首版成功率**：尽量复制 ST 参考布局，不从零猜匹配参数。
2. **天线环境**：主板铜皮、金属定位板、电池、排线、插座和外壳都会影响性能。
3. **可调试性**：预留 π 型匹配位置、RF 测试点和完整 SWD 接口。
4. **USB 电源域**：正确处理 VDDUSB 和 VBUS 检测。
5. **插接结构影响 RF**：两种模组对比时应保持相同高度、净空和机械结构。

### FOC 旋钮

- FOC 电流环由独立 MCU 或集成电机控制器运行。
- 主控只发送目标参数，并接收角度、速度、按压和故障事件。
- FOC 功率级、相线和开关节点远离天线、USB、触控和分体通信线路。

---

## 6. 分体通信

### 分离模式

```text
副半边 BLE Peripheral
        ↓ 自定义 GATT
主机半边 BLE Central
        ↓
USB HID 或 BLE HID → 电脑
```

副半边发送：

- 按键变化事件；
- 周期性完整按键位图；
- 旋钮、触摸和电池状态。

### 合体模式

- 优先使用 UART + 半双工 RS-485，以提高抗插拔和抗 FOC 噪声能力。
- RS-485 收发器放在公共主板和副半边主板，计算模组只提供 UART 与方向控制。
- BLE 和有线链路使用相同的上层消息格式。

### 链路切换

```text
检测合体
→ 开启有线收发器
→ 握手
→ 完整状态同步
→ 确认有线稳定
→ 挂起副半边 BLE

检测分离
→ 清除副半边残留按键
→ 启动定向广播
→ BLE 重连
→ 完整状态同步
→ 恢复事件流
```

任何断线或切换都必须处理 release-all 和状态重建，避免卡键。

---

## 7. 软件核心架构

软件只保留三层核心抽象：

```text
输入层
├─ 主/副半边扫描与消抖
├─ 触控、旋钮和其他输入事件
└─ 全键盘权威状态

传输层
├─ split_ble_transport
└─ split_wired_transport

输出层
├─ usb_hid_output
└─ ble_hid_output
```

关键原则：

- 上层键位、层、宏和输入事件模型不直接调用 ESP-IDF 或 STM32 HAL。
- USB 与 BLE 共用同一套逻辑输入状态。
- 平台相关部分仅限 USB 栈、BLE 栈、低功耗、定时器和外设驱动。
- 副半边只上传内部事件；最终 HID 报告始终由主机半边生成。

建议保留的 HID 类型：

- Keyboard（NKRO，并考虑 Boot Protocol）；
- Consumer Control；
- Mouse/Touch；
- Vendor-defined 配置与维护接口。

---

## 8. 验证与选型

两种主控必须在相同主板、相同外围和尽可能一致的连接参数下比较。

### 核心指标

- **功耗：**BLE 单连接、双连接、持续输入、合体、USB 和待机。
- **延迟：**本地按键、副半边按键、跨半边组合键、链路切换。
- **稳定性：**长时间连接、丢包、卡键、USB 枚举和重连。
- **RF：**实际外壳、金属板、电池和人体附近的距离与 RSSI。
- **资源：**Flash、RAM、任务栈和扩展余量。
- **开发成本：**硬件改版次数、Bring-up 难度和工具链复杂度。

### 决策规则

- ESP32-S3 在目标电池容量下满足续航，并且功能仍在快速扩展：继续使用 ESP32-S3。
- ESP32-S3 双 BLE 实测续航不达标，且 STM32WB55 的资源足够：转向 STM32WB55。
- 复杂屏幕超出 STM32WB55 能力：将显示独立为协处理器，不改变键盘连接核心。

---

## 9. 推荐实施顺序

1. 冻结功能级模组接口。
2. 在 STM32CubeMX 中先完成 WB55 引脚映射，再映射 ESP32-S3。
3. 制作公共主板和 ESP32-S3 插接模组，建立完整功能基线。
4. 制作裸 STM32WB55 自研模组，先独立完成电源、SWD、晶振、USB 和 BLE Bring-up。
5. 将两种模组插入同一主板，进行功耗、延迟、稳定性和 RF 对比。
6. 根据实测结果选择最终产品主控。

---

## 10. 快速结论

- ESP32-S3 没有功能性障碍，主要问题是无线功耗。
- STM32WB55 的价值是低功耗，不是功能更多或开发更简单。
- 公共主板加可插拔计算模组是合理的迭代方案。
- 模组应做到机械兼容和功能级接口兼容，不追求任意 GPIO 逐脚等价。
- 裸 STM32WB55 模组最关键的难点是射频、天线环境、USB 电源域和可调试性。
- 最终选型必须基于同板实测，而不是只比较数据手册峰值。
